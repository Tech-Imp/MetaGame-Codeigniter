// Generated by CoffeeScript 1.12.4
(function() {
  var adminSectionDelete,
    bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  adminSectionDelete = (function(superClass) {
    extend(adminSectionDelete, superClass);

    adminSectionDelete.prototype.debug = true;

    function adminSectionDelete() {
      this.removeSection = bind(this.removeSection, this);
      this.setupEvents = bind(this.setupEvents, this);
      if (this.debug) {
        console.log("adminSectionDelete.constructor");
      }
      adminSectionDelete.__super__.constructor.call(this);
      this.setupEvents();
    }

    adminSectionDelete.prototype.setupEvents = function() {
      if (this.debug) {
        console.log("adminSectionDelete.setupEvents");
      }
      return $("#permDelete").unbind().bind("click", (function(_this) {
        return function(event) {
          $("#permDelete").prop("disabled", "disabled");
          return _this.removeSection();
        };
      })(this));
    };

    adminSectionDelete.prototype.removeSection = function() {
      if (this.debug) {
        console.log("adminSectionDelete.removeSection");
      }
      return $.ajax({
        url: this.base_url + "/admin/securepost/deleteSection",
        type: 'post',
        dataType: 'json',
        data: {
          sectionID: $('#assocID').val()
        },
        success: (function(_this) {
          return function(response) {
            if (response.success) {
              console.log("Success");
              return location.reload();
            } else if (response.debug) {
              return console.log("debug");
            } else if (response.error) {
              return _this.textBodyResponse(response.error, "#userMessage", true, "#textArea-alert");
            }
          };
        })(this)
      });
    };

    return adminSectionDelete;

  })(window.classes.dashboardIndex);

  if (window.classes == null) {
    window.classes = {};
  }

  window.classes.adminSectionDelete = adminSectionDelete;

  if (window.objs == null) {
    window.objs = {};
  }

  $(document).ready(function() {
    return window.objs.adminSectionDelete = new adminSectionDelete;
  });

}).call(this);
