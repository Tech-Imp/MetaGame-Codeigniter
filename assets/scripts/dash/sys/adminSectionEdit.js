// Generated by CoffeeScript 1.12.4
(function() {
  var adminSectionEdit,
    bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  adminSectionEdit = (function(superClass) {
    extend(adminSectionEdit, superClass);

    adminSectionEdit.prototype.debug = true;

    function adminSectionEdit() {
      this.saveSection = bind(this.saveSection, this);
      this.setupEvents = bind(this.setupEvents, this);
      if (this.debug) {
        console.log("adminSectionEdit.constructor");
      }
      adminSectionEdit.__super__.constructor.call(this);
      this.setupEvents();
    }

    adminSectionEdit.prototype.setupEvents = function() {
      if (this.debug) {
        console.log("adminSectionEdit.setupEvents");
      }
      $("#sectionUsage").tinymce({
        script_url: this.base_url + '/assets/scripts/tinymce/tinymce.min.js',
        theme: "modern",
        plugins: ["advlist autolink lists link image charmap print preview anchor", "searchreplace visualblocks code fullscreen", "insertdatetime media table contextmenu paste"],
        toolbar: "insertfile undo redo | styleselect | bold italic | alignleft aligncenter alignright alignjustify | bullist numlist outdent indent | link image"
      });
      $("#saveEdits").unbind().bind("click", (function(_this) {
        return function(event) {
          $("#saveEdits").prop("disabled", "disabled");
          return _this.saveSection();
        };
      })(this));
      return $('#sectionController').keyup(function() {
        var empty;
        empty = false;
        $('.textReq').each(function() {
          if ($(this).val() === '') {
            return empty = true;
          }
        });
        if (empty) {
          console.log("remain locked");
          return $("#saveEdits").prop("disabled", "disabled");
        } else {
          console.log("open");
          return $("#saveEdits").prop("disabled", false);
        }
      });
    };

    adminSectionEdit.prototype.saveSection = function() {
      if (this.debug) {
        console.log("adminSectionEdit.saveSection");
      }
      if ($.trim($("#sectionUsage").html())) {
        return $.ajax({
          url: this.base_url + "/admin/securepost/editSection",
          type: 'post',
          dataType: 'json',
          data: {
            id: $("#assocID").val(),
            usage: $('#sectionUsage').html(),
            visibility: $("#linkVis").val(),
            where: $("#linkLoc").val()
          },
          success: (function(_this) {
            return function(response) {
              if (response.success) {
                console.log("Success");
                _this.textBodyResponse("Section updated", "#sectionMessage", false, "#sectionArea-alert", "#saveEdits");
                return $("#saveEdits").prop("disabled", false);
              } else if (response.debug) {
                console.log("debug");
                return $("#saveEdits").prop("disabled", false);
              } else if (response.error) {
                console.log("error");
                _this.textBodyResponse(response.error, "#sectionMessage", true, "#sectionArea-alert", "#saveEdits");
                return $("#saveEdits").prop("disabled", false);
              }
            };
          })(this)
        });
      } else {
        return this.textBodyResponse("You need fill in Usage field!", "#sectionMessage", true, "#sectionArea-alert", "#saveEdits");
      }
    };

    return adminSectionEdit;

  })(window.classes.dashboardIndex);

  if (window.classes == null) {
    window.classes = {};
  }

  window.classes.adminSectionEdit = adminSectionEdit;

  if (window.objs == null) {
    window.objs = {};
  }

  $(document).ready(function() {
    return window.objs.adminSectionEdit = new adminSectionEdit;
  });

}).call(this);
