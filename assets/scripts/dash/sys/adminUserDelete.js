// Generated by CoffeeScript 1.12.7
(function() {
  var adminUserDelete,
    bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  adminUserDelete = (function(superClass) {
    extend(adminUserDelete, superClass);

    adminUserDelete.prototype.debug = true;

    function adminUserDelete() {
      this.removeUser = bind(this.removeUser, this);
      this.setupEvents = bind(this.setupEvents, this);
      if (this.debug) {
        console.log("adminUserDelete.constructor");
      }
      adminUserDelete.__super__.constructor.call(this);
      this.setupEvents();
    }

    adminUserDelete.prototype.setupEvents = function() {
      if (this.debug) {
        console.log("adminUserDelete.setupEvents");
      }
      return $("#permDelete").unbind().bind("click", (function(_this) {
        return function(event) {
          $("#permDelete").prop("disabled", "disabled");
          return _this.removeUser();
        };
      })(this));
    };

    adminUserDelete.prototype.removeUser = function() {
      if (this.debug) {
        console.log("adminUserDelete.removeUser");
      }
      return $.ajax({
        url: this.base_url + "/admin/securepost/deleteUserFromSection",
        type: 'post',
        dataType: 'json',
        data: {
          entryID: $('#assocID').val()
        },
        success: (function(_this) {
          return function(response) {
            if (response.success) {
              console.log("Success");
              return location.reload();
            } else if (response.debug) {
              return console.log("debug");
            } else if (response.error) {
              return _this.textBodyResponse(response.error, "#userMessage", true, "#textArea-alert");
            }
          };
        })(this)
      });
    };

    return adminUserDelete;

  })(window.classes.dashboardIndex);

  if (window.classes == null) {
    window.classes = {};
  }

  window.classes.adminUserDelete = adminUserDelete;

  if (window.objs == null) {
    window.objs = {};
  }

  $(document).ready(function() {
    return window.objs.adminUserDelete = new adminUserDelete;
  });

}).call(this);
