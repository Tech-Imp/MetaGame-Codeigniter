// Generated by CoffeeScript 1.12.4
(function() {
  var dashboardUpdateSocial,
    bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  dashboardUpdateSocial = (function(superClass) {
    extend(dashboardUpdateSocial, superClass);

    dashboardUpdateSocial.prototype.debug = true;

    function dashboardUpdateSocial() {
      this.deleteMedia = bind(this.deleteMedia, this);
      this.saveEdits = bind(this.saveEdits, this);
      this.setupEvents = bind(this.setupEvents, this);
      if (this.debug) {
        console.log("dashboardUpdateSocial.constructor");
      }
      dashboardUpdateSocial.__super__.constructor.call(this);
      this.setupEvents();
    }

    dashboardUpdateSocial.prototype.setupEvents = function() {
      if (this.debug) {
        console.log("dashboardUpdateSocial.setupEvents");
      }
      $("#saveEdits").unbind().bind("click", (function(_this) {
        return function(event) {
          return _this.saveEdits();
        };
      })(this));
      return $("#permDelete").unbind().bind("click", (function(_this) {
        return function(event) {
          return _this.deleteMedia();
        };
      })(this));
    };

    dashboardUpdateSocial.prototype.saveEdits = function() {
      if (this.debug) {
        console.log("dashboardUpdateSocial.saveEdits");
      }
      return $.ajax({
        url: this.base_url + "/admin/post/postprofile/editSocial",
        type: 'post',
        dataType: 'json',
        data: {
          infoID: $('#profileID').val(),
          logoID: $('#logoUsed').val(),
          facebook: $('#facebookSocial').val(),
          youtube: $('#youtubeSocial').val(),
          twitter: $('#twitterSocial').val(),
          tumblr: $('#tumblrSocial').val(),
          email: $('#emailSocial').val(),
          twitch: $('#twitchSocial').val(),
          bodyText: $('#mceSocial').html(),
          section: $('#section').val(),
          exFlag: $('#exclusiveFlag').val()
        },
        success: (function(_this) {
          return function(response) {
            if (response.success) {
              return _this.textBodyResponse("Edit saved to database", "#userMessage", false, "#textArea-alert");
            } else if (response.debug) {
              return console.log("debug");
            } else if (response.error) {
              return _this.textBodyResponse(response.error, "#userMessage", true, "#textArea-alert");
            }
          };
        })(this)
      });
    };

    dashboardUpdateSocial.prototype.deleteMedia = function() {
      if (this.debug) {
        console.log("dashboardUpdateSocial.deleteMedia");
      }
      return $.ajax({
        url: this.base_url + "/admin/post/postprofile/deleteSocial",
        type: 'post',
        dataType: 'json',
        data: {
          infoID: $('#profileID').val()
        },
        success: (function(_this) {
          return function(response) {
            if (response.success) {
              console.log("Success");
              return location.reload();
            } else if (response.debug) {
              return console.log("debug");
            } else if (response.error) {
              return _this.textBodyResponse(response.error, "#userMessage", true, "#textArea-alert");
            }
          };
        })(this)
      });
    };

    return dashboardUpdateSocial;

  })(window.classes.dashboardIndex);

  if (window.classes == null) {
    window.classes = {};
  }

  window.classes.dashboardUpdateSocial = dashboardUpdateSocial;

  if (window.objs == null) {
    window.objs = {};
  }

  $(document).ready(function() {
    return window.objs.dashboardUpdateSocial = new dashboardUpdateSocial;
  });

}).call(this);
